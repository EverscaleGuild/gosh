# syntax=127.0.0.1:5000/goshfile

apiVersion: 1
image: bash:latest
steps:
  - name: print hello
    run:
      command: ["/usr/local/bin/bash"]
      args:
        - -c
        - >-
          echo "Hi there" > /message.txt
